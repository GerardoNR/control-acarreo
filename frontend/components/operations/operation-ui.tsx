import type { ReactNode } from 'react';
export const money = (value: string | number) => new Intl.NumberFormat('es-MX', { style: 'currency', currency: 'MXN' }).format(Number(value));
export const quantity = (value: string | number, unit?: string) => `${Number(value).toLocaleString('es-MX', { minimumFractionDigits: 3, maximumFractionDigits: 3 })}${unit ? ` ${unit}` : ''}`;
export function ProgressBar({ value }: { value: number }) { const width = Math.min(Math.max(value, 0), 100); return <div className="min-w-36" aria-label={`Avance ${value.toFixed(2)}%`}><div className="h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-[#2563EB]" style={{ width: `${width}%` }} /></div><span className="mt-1 block text-xs font-semibold text-[#475569]">{value.toLocaleString('es-MX', { maximumFractionDigits: 2 })}%</span></div>; }
export function MetricCard({ label, value, detail }: { label: string; value: ReactNode; detail?: string }) { return <div className="rounded-xl border border-[#CBD5E1] bg-white p-4"><p className="text-xs font-semibold uppercase tracking-wide text-[#64748B]">{label}</p><p className="mt-2 text-2xl font-semibold text-[#0F172A]">{value}</p>{detail ? <p className="mt-1 text-xs text-[#64748B]">{detail}</p> : null}</div>; }
export function StateBadge({ children }: { children: ReactNode }) { return <span className="inline-flex whitespace-nowrap rounded-full bg-blue-50 px-2 py-1 text-xs font-semibold text-blue-700 ring-1 ring-inset ring-blue-200">{children}</span>; }
